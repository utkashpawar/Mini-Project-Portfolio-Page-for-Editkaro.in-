/* =========================================================
   EDITKARO.IN — Design tokens
   Palette born from color grading: shadows push teal,
   highlights push orange — the classic film LUT split-tone.
   ========================================================= */
:root{
  --bg:            #0b0b0d;
  --panel:         #16161a;
  --panel-2:       #1e1e24;
  --line:          #2a2a31;
  --text:          #f5f5f0;
  --text-muted:    #8a8a93;
  --orange:        #ff5a36;
  --orange-dim:    #c8431f;
  --teal:          #2dd4bf;
  --teal-dim:      #1f9c8c;

  --display: 'Bebas Neue', Impact, sans-serif;
  --body:    'Inter', system-ui, sans-serif;
  --mono:    'IBM Plex Mono', ui-monospace, monospace;

  --radius: 14px;
  --ease: cubic-bezier(.16,.84,.44,1);
}

*,*::before,*::after{ box-sizing:border-box; }
html{ scroll-behavior:smooth; }
body{
  margin:0;
  background:var(--bg);
  color:var(--text);
  font-family:var(--body);
  line-height:1.5;
  -webkit-font-smoothing:antialiased;
}
img,video{ max-width:100%; display:block; }
a{ color:inherit; text-decoration:none; }
button{ font:inherit; color:inherit; background:none; border:0; cursor:pointer; }
.mono{ font-family:var(--mono); letter-spacing:.04em; }

a:focus-visible, button:focus-visible{
  outline:2px solid var(--teal);
  outline-offset:3px;
  border-radius:4px;
}

@media (prefers-reduced-motion: reduce){
  *,*::before,*::after{ animation-duration:0.001ms !important; animation-iteration-count:1 !important; transition-duration:0.001ms !important; scroll-behavior:auto !important; }
}

/* ================= NAV ================= */
.nav{
  position:fixed; inset:0 0 auto 0; z-index:100;
  background:rgba(11,11,13,.7);
  backdrop-filter:blur(10px);
  border-bottom:1px solid var(--line);
}
.nav__inner{
  max-width:1240px; margin:0 auto; padding:16px 24px;
  display:flex; align-items:center; justify-content:space-between;
}
.nav__logo{
  font-family:var(--display); font-size:22px; letter-spacing:.03em;
}
.nav__logo .dot{ color:var(--orange); }
.nav__links{ display:flex; align-items:center; gap:28px; font-size:14px; font-weight:500; }
.nav__links a{ color:var(--text-muted); transition:color .2s var(--ease); }
.nav__links a:hover{ color:var(--text); }
.nav__cta{
  color:var(--bg) !important;
  background:var(--teal);
  padding:9px 16px; border-radius:100px; font-weight:600;
}
.nav__cta:hover{ background:#54e0cd; }

.nav__burger{ display:none; flex-direction:column; gap:5px; padding:6px; }
.nav__burger span{ width:22px; height:2px; background:var(--text); display:block; transition:transform .25s var(--ease), opacity .25s var(--ease); }

@media (max-width:820px){
  .nav__burger{ display:flex; }
  .nav__links{
    position:fixed; top:64px; left:0; right:0;
    background:var(--panel); border-bottom:1px solid var(--line);
    flex-direction:column; align-items:flex-start; padding:20px 24px;
    gap:18px;
    transform:translateY(-8px); opacity:0; pointer-events:none;
    transition:transform .25s var(--ease), opacity .25s var(--ease);
  }
  .nav__links.is-open{ transform:translateY(0); opacity:1; pointer-events:auto; }
  .nav__cta{ width:100%; text-align:center; }
}

/* ================= HERO ================= */
.hero{
  position:relative; overflow:hidden;
  padding:150px 24px 0;
  display:flex; flex-direction:column; align-items:center; text-align:center;
}
.hero__grain{
  position:absolute; inset:0; pointer-events:none; opacity:.05;
  background-image:radial-gradient(circle at 1px 1px, #fff 1px, transparent 0);
  background-size:3px 3px;
}
.hero::before{
  content:''; position:absolute; top:-20%; left:50%; translate:-50% 0;
  width:900px; height:900px; border-radius:50%;
  background:radial-gradient(circle, rgba(255,90,54,.16), transparent 60%);
  pointer-events:none;
}
.hero::after{
  content:''; position:absolute; bottom:10%; left:15%;
  width:500px; height:500px; border-radius:50%;
  background:radial-gradient(circle, rgba(45,212,191,.12), transparent 65%);
  pointer-events:none;
}
.hero__inner{ position:relative; max-width:820px; }
.hero__eyebrow{
  font-size:12px; color:var(--text-muted); margin:0 0 22px;
  display:flex; gap:10px; align-items:center; justify-content:center;
}
.rec{ color:var(--orange); animation:blink 1.4s infinite; }
@keyframes blink{ 50%{ opacity:.3; } }
.hero__title{
  font-family:var(--display); font-weight:400;
  font-size:clamp(42px, 8vw, 92px);
  line-height:.98; letter-spacing:.01em;
  margin:0 0 24px;
}
.hero__title--accent{
  background:linear-gradient(90deg, var(--orange), var(--teal));
  -webkit-background-clip:text; background-clip:text; color:transparent;
}
.hero__sub{
  color:var(--text-muted); font-size:17px; max-width:560px; margin:0 auto 36px;
}
.hero__actions{ display:flex; gap:14px; justify-content:center; flex-wrap:wrap; margin-bottom:70px; }

.btn{
  padding:13px 26px; border-radius:100px; font-weight:600; font-size:14px;
  transition:transform .2s var(--ease), background .2s var(--ease), color .2s var(--ease);
  display:inline-block;
}
.btn--primary{ background:var(--orange); color:#fff; }
.btn--primary:hover{ background:#ff7657; transform:translateY(-2px); }
.btn--ghost{ border:1px solid var(--line); color:var(--text); }
.btn--ghost:hover{ border-color:var(--teal); color:var(--teal); }

.marquee{
  width:100%; border-top:1px solid var(--line); border-bottom:1px solid var(--line);
  overflow:hidden; padding:16px 0; background:var(--panel);
}
.marquee__track{
  display:flex; gap:40px; white-space:nowrap; width:max-content;
  font-family:var(--display); font-size:20px; letter-spacing:.05em;
  color:var(--text-muted);
  animation:scroll 26s linear infinite;
}
.marquee__track span::after{ content:'✦'; margin-left:40px; color:var(--orange); font-size:12px; }
@keyframes scroll{ from{ transform:translateX(0); } to{ transform:translateX(-50%); } }

/* ================= TIMELINE FILTER ================= */
.timeline{ max-width:1240px; margin:0 auto; padding:64px 24px 12px; }
.timeline__label{ font-size:12px; color:var(--text-muted); margin:0 0 16px; }
.timeline__track{
  position:relative;
  display:flex; gap:10px; flex-wrap:wrap;
  padding-top:18px; border-top:2px dashed var(--line);
}
.timeline__playhead{ display:none; } /* decorative hook kept minimal for real devices */

.chip{
  padding:10px 18px; border-radius:8px; font-size:13px; font-weight:600;
  background:var(--panel); border:1px solid var(--line); color:var(--text-muted);
  transition:all .2s var(--ease);
  position:relative;
}
.chip::before{
  content:'▸'; margin-right:6px; color:var(--teal); opacity:0;
  transition:opacity .2s var(--ease);
}
.chip:hover{ color:var(--text); border-color:var(--teal); }
.chip.is-active{
  background:var(--teal); border-color:var(--teal); color:#06211d;
}
.chip.is-active::before{ opacity:1; color:#06211d; }

/* ================= WORK GRID ================= */
.work{ max-width:1240px; margin:0 auto; padding:40px 24px 100px; }
.work__grid{
  display:grid; grid-template-columns:repeat(auto-fill, minmax(280px, 1fr)); gap:22px;
}
.work__empty{ text-align:center; color:var(--text-muted); padding:60px 0; }

.card{
  background:var(--panel); border:1px solid var(--line); border-radius:var(--radius);
  overflow:hidden; cursor:pointer;
  transition:transform .3s var(--ease), border-color .3s var(--ease);
}
.card:hover{ transform:translateY(-6px); border-color:var(--orange); }
.card__thumb{
  aspect-ratio:9/13; position:relative; overflow:hidden;
  display:flex; align-items:flex-end;
}
.card__thumb-bg{
  position:absolute; inset:0;
  transition:transform .5s var(--ease);
}
.card:hover .card__thumb-bg{ transform:scale(1.08); }
.card__thumb::after{
  content:''; position:absolute; inset:0;
  background:linear-gradient(180deg, transparent 40%, rgba(0,0,0,.85) 100%);
}
.card__play{
  position:absolute; top:50%; left:50%; translate:-50% -50%;
  width:52px; height:52px; border-radius:50%;
  background:rgba(11,11,13,.55); border:1px solid rgba(245,245,240,.4);
  display:flex; align-items:center; justify-content:center;
  opacity:0; scale:.85; transition:opacity .25s var(--ease), scale .25s var(--ease);
  z-index:2; backdrop-filter:blur(4px);
}
.card:hover .card__play{ opacity:1; scale:1; }
.card__play svg{ width:16px; height:16px; fill:#fff; margin-left:2px; }

.card__timecode{
  position:absolute; top:12px; left:12px; z-index:2;
  font-size:11px; padding:4px 8px; border-radius:5px;
  background:rgba(0,0,0,.5); color:var(--teal);
}
.card__tag{
  position:absolute; top:12px; right:12px; z-index:2;
  font-size:10px; padding:4px 9px; border-radius:100px;
  background:var(--orange); color:#fff; font-weight:700; letter-spacing:.04em;
}
.card__body{ position:relative; z-index:2; padding:14px 16px; margin-top:-46px; }
.card__title{ font-weight:600; font-size:14.5px; margin:0 0 2px; }
.card__meta{ font-size:12px; color:var(--text-muted); margin:0; }

/* ================= ABOUT ================= */
.about{
  max-width:1240px; margin:0 auto; padding:80px 24px;
  display:grid; grid-template-columns:1.3fr 1fr; gap:60px; align-items:center;
  border-top:1px solid var(--line);
}
.about__copy h2{
  font-family:var(--display); font-weight:400; font-size:clamp(28px,4vw,42px);
  line-height:1.05; margin:0 0 18px;
}
.about__copy p{ color:var(--text-muted); font-size:16px; max-width:480px; }
.about__stats{ display:flex; gap:32px; }
.about__stats span{ display:block; font-size:38px; color:var(--orange); font-weight:600; }
.about__stats small{ color:var(--text-muted); font-size:12px; }

@media (max-width:820px){
  .about{ grid-template-columns:1fr; }
}

/* ================= FOOTER ================= */
.footer{ background:var(--panel); border-top:1px solid var(--line); padding:80px 24px 30px; }
.footer__inner{
  max-width:1240px; margin:0 auto;
  display:flex; justify-content:space-between; align-items:flex-end; gap:40px; flex-wrap:wrap;
  padding-bottom:50px; border-bottom:1px solid var(--line);
}
.footer__eyebrow{ color:var(--teal); font-size:12px; margin:0 0 14px; }
.footer__title{
  font-family:var(--display); font-weight:400; font-size:clamp(30px,5vw,54px);
  line-height:1; margin:0 0 26px;
}
.footer__meta p{ font-size:12px; color:var(--text-muted); margin:0 0 12px; }
.footer__links{ display:flex; flex-direction:column; gap:8px; }
.footer__links a{ color:var(--text); font-weight:500; transition:color .2s; }
.footer__links a:hover{ color:var(--orange); }
.footer__legal{ max-width:1240px; margin:22px auto 0; font-size:11px; color:var(--text-muted); }

/* ================= LIGHTBOX ================= */
.lightbox{
  position:fixed; inset:0; z-index:200;
  display:flex; align-items:center; justify-content:center; padding:24px;
  opacity:0; pointer-events:none; transition:opacity .25s var(--ease);
}
.lightbox.is-open{ opacity:1; pointer-events:auto; }
.lightbox__backdrop{ position:absolute; inset:0; background:rgba(6,6,7,.85); backdrop-filter:blur(6px); }
.lightbox__panel{
  position:relative; z-index:2; width:min(920px, 100%);
  background:var(--panel); border:1px solid var(--line); border-radius:var(--radius);
  display:grid; grid-template-columns:1fr 1fr; overflow:hidden;
  transform:translateY(16px) scale(.98); transition:transform .3s var(--ease);
  max-height:88vh;
}
.lightbox.is-open .lightbox__panel{ transform:translateY(0) scale(1); }
.lightbox__frame{ aspect-ratio:9/13; position:relative; }
.lightbox__info{ padding:32px; display:flex; flex-direction:column; justify-content:center; }
.lightbox__tag{ color:var(--teal); font-size:11px; margin:0 0 10px; }
.lightbox__desc{ color:var(--text-muted); font-size:14.5px; margin-top:12px; }
.lightbox__close{
  position:absolute; top:14px; right:14px; z-index:3;
  width:34px; height:34px; border-radius:50%;
  background:rgba(0,0,0,.5); color:#fff; font-size:14px;
}
@media (max-width:720px){
  .lightbox__panel{ grid-template-columns:1fr; overflow-y:auto; }
}
